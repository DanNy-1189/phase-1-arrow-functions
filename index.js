function divide(a, b) {
    a = 2000
    b = 100
    let divide = (a/b);
    return divide;
};

const square = parameter => parameter * parameter;

const add = (parameter1, parameter2) => parameter1 + parameter2;